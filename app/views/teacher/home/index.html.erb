<div class="box">
  <div class="content">
    <input type="hidden" id="url-events" value=<%= events_teacher_home_index_path %> />
    <div class="home-header">
      <h3>Oi, <%= current_user.label_name %>!</h3>
      <p>
        Fique de olho nas útimas atualizações e em seus próximos compromissos!
      </p>
    </div>
    <br>
    <div class="row">
      <div class="col-md-7">
        <h5>Notificações</h4>
        <div class="notifications-list">
          <%= link_to teacher_home_index_path do %>
            <div id="new-notification-alert"></div>
          <% end %>

          <% if @notifications.each do |notification| %>
            <%= render "teacher/notifications/#{notification.notifiable_type.underscore}/#{notification.action}", notification: notification %>
          <% end.empty? %>
            <div class="empty-notification">
              Nenhuma notificação.
            </div>
          <% end %>

          <% if current_user.notifications.count > 5 %>
            <%= link_to 'Ver mais', teacher_notifications_path, class: 'btn btn-light' %>
          <% end %>
        </div>
      </div>
      <div class="col-md-5">
        <h5>Seus compromissos</h4>
        <div id="calendar"></div>
      </div>
    </div>
  </div>
</div>
