<div class="box">
  <div class="content">
    <input type="hidden" id="url-events" value=<%= events_teacher_home_index_path %> />
    <div class="home-header">
      <h3>Oi, <%= current_user.label_name %>!</h3>
      <p>
        Fique de olho nas útimas atualizações e em seus próximos compromissos!
      </p>
    </div>
    <br>
    <div class="row">
      <div class="col-md-7">
        <h5>Notificações</h4>
        <div class="notifications-list">
          <% @notifications.each do |notification| %>
            <%= render "teacher/notifications/#{notification.notifiable.class.name.sub('::', '_').downcase}/#{notification.action}", notification: notification %>
          <% end %>

          <% if current_user.notifications.count > 5 %>
            <%= link_to 'Ver mais', teacher_notifications_path, class: 'btn btn-light' %>
          <% end %>
        </div>
      </div>
      <div class="col-md-5">
        <h5>Seus compromissos</h4>
        <div id="calendar"></div>
      </div>
    </div>
  </div>
</div>
