<div class="list-header">
    <%= link_to new_student_work_work_version_path(work_id: @work.id), class: 'btn btn-success', remote: true do %>
      Adicionar
    <% end %>
    <br>
    <%= form_tag student_work_path(id: @work.id), autocomplete: :off, method: :get, remote: true do %>
      <%= text_field_tag 'search', params[:search], placeholder: "Pesquisar", class: "form-control col-md-12 col-sm-12 col-12", id: "search-input" %>
    <% end %>
</div>
<br>
