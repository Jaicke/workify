<div class="box">
  <div class="content">
    <%= render 'student/works/tabs' %>
    <%= render 'student/discussions/shared/header' %>

    <div class="discussion-list">
      <% @discussions.each do |discussion| %>
        <%= render 'student/discussions/shared/discussion_card', discussion: discussion %>
      <% end %>
    </div>

    <div class="info-pagination">
      <%= render "layouts/shared/pagination", items: @discussions %>
    </div>
  </div>
</div>
